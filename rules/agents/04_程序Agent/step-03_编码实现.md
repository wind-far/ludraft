---
# 注意不要修改本文头文件，如修改，CodeBuddy（内网版）将按照默认逻辑设置
type: manual
---
# Step 03: 编码实现 [MODE: IMPLEMENT]

## 📍 当前位置
步骤 3/4 | 前置：任务清单已创建 | 后续：完成检查 | 💻 实现模式

## 🎯 本步骤目标
按任务清单逐个编码，应用最佳实践，避免常见陷阱。

## ⚡ 最小必要上下文

> 本步骤只需以下信息，**请勿读取不在列表中的文件**：

| 文件 | 需要读取的部分 | 说明 |
|------|---------------|------|
| `05_任务清单.md` | 📖 完整阅读 | 编码的核心指引 |
| `03_技术设计.md` | 🗜️ 只读当前任务涉及的**架构和接口定义** | 不需要重新读完整个文档 |
| `01_策划案.md` | 🗜️ 只读当前任务关联的**验收标准** | 编码时对照AC |
| `templates/代码规范.md` | 按需加载 | 不确定规范时查阅 |
| ❌ `全局策划案.md` | **不需要** | 编码阶段不需要全局视图 |
| ❌ `02_UX设计.md` | **不需要**（除非当前任务涉及UI布局） | 仅UI任务时按需读取 |

## 📋 执行清单

1. □ **加载防护规则** — 阅读 `rule_guards.md` 中的事前防护规则（G-001~G-010）
2. □ **按任务清单顺序逐个编码**
3. □ 每完成一个任务，**更新任务清单状态**
4. □ 遇到问题时，**先查询知识库**的问题解决方案
5. □ 编码时应用以下最佳实践：
   - 所有外部输入和引用必须检查null
   - 每完成一个小功能就自测
   - 复杂逻辑必须添加注释
6. □ 编码时避免以下常见陷阱：
   - 使用魔法数字和硬编码字符串
   - 不处理可能的异常
   - 在Update中频繁分配内存
7. □ 如有新发现，**更新知识库**

## 🛡️ 事前防护检查（每次编辑文件前自动执行）

> **来源**: `rule_guards.md` | **执行方式**: 每次修改/创建/删除文件前，自动匹配以下规则

**🔴 阻断规则（触发即停止操作）**：
- [ ] **[G-001]** 文件路径不含 `.meta` → 🚫 触发则阻断
- [ ] **[G-002]** 文件不在 `Library/` 或 `Temp/` 目录 → 🚫 触发则阻断
- [ ] **[G-006]** 删除文件前，确认无其他文件引用 → 🚫 触发则阻断
- [ ] **[G-008]** 安装新Package前，确认用户已同意 → 🚫 触发则阻断

**🟡 警告规则（输出警告后继续）**：
- [ ] **[G-003]** 新建 `.cs` 文件时，命名空间符合项目规范
- [ ] **[G-005]** 修改 MonoBehaviour 时，外部引用有 null 检查
- [ ] **[G-007]** 修改公共接口时，搜索并列出受影响的调用方
- [ ] **[G-004]** 修改 ScriptableObject 时，检查序列化数据兼容
- [ ] **[G-010]** 修改 ProjectSettings 时，提醒检查影响范围

**输出格式**：触发时输出 `🛡️ [G-XXX] {🚫阻断/⚠️警告}：{原因}`

## 📦 可选技能加载（按需）

> **来源**: `skills/` 目录 | **执行方式**: 根据当前任务涉及的技术领域，按需加载对应技能包

| 任务特征 | 加载技能包 | 路径 |
|---------|-----------|------|
| 涉及 MonoBehaviour 编写 | MonoBehaviour 生命周期最佳实践 | `skills/unity/monobehaviour-patterns.md` |
| 涉及 UI 开发（面板/组件） | UGUI / UI Toolkit 模式 | `skills/unity/ui-toolkit-patterns.md` |
| 涉及 null 安全/外部引用 | C# 空安全最佳实践 | `skills/csharp/null-safety.md` |
| 涉及事件系统/模块通信 | 事件系统设计模式 | `skills/architecture/event-system.md` |
| 涉及性能优化 | Unity 性能优化模式 | `skills/unity/performance-patterns.md` |
| 涉及状态管理 | 状态机设计模式 | `skills/architecture/state-machine.md` |
| 涉及异步/协程 | Unity 中的异步编程 | `skills/csharp/async-await-unity.md` |

**加载规则**：
- 只加载与当前任务**直接相关**的技能包（通常1-2个）
- 技能包内容作为编码参考，结合项目知识库的特定经验一起使用
- 技能包中的**检查清单**在编码完成后用于自查

## 📝 代码规范速查
- 参考 `templates/代码规范.md` 获取完整规范
- 命名空间: 功能名称
- 类名: PascalCase
- 私有字段: _camelCase
- 常量: MAX_VALUE 或 PascalCase
- 公有方法须有XML注释

## 🔧 场景创建工具同步规则

**触发条件**: 当需求涉及UI组件时，必须同步更新场景创建工具

| 触发情况 | 需要更新的内容 |
|----------|----------------|
| 新增UI面板/组件 | 在SceneSetupTool中添加创建代码 |
| 修改现有UI组件结构 | 同步修改SceneSetupTool |
| 新增UI组件引用关系 | 在SetupReferences中添加引用设置 |
| 删除UI组件 | 从SceneSetupTool中移除对应代码 |

**工具位置**: `Assets/Dialogue/Editor/SceneSetupTool.cs`

## ⚠️ 本步骤常见陷阱
- 编码完成不自测
- 忘记更新任务清单状态
- 只关注正常流程，不处理异常情况

## 🔔 事后防护检查（编码全部完成后自动执行）

> **来源**: `rule_guards.md` | **执行方式**: 所有任务编码完成后、进入step-04前，自动执行

- [ ] **[G-101]** Debug.Log 残留检查 — 搜索非必要日志输出
- [ ] **[G-102]** TODO/FIXME/HACK 检查 — 列出所有待处理项
- [ ] **[G-103]** Manager/Service 类变更 → 提示更新全局技术文档
- [ ] **[G-104]** 数据结构变更 → 提示检查序列化兼容性
- [ ] **[G-106]** UI组件变更 → 提示同步 SceneSetupTool
- [ ] **[G-107]** 事件订阅变更 → 检查 OnDestroy 中是否有对应取消

**输出格式**：
```
🔔 [事后检查] 编码完成，自动执行防护检查：
  ✅/⚠️ [G-XXX] {检查项} — {结果}
📊 事后检查结果：X通过 / X警告 / X阻断
```

## ✅ 完成标志
- [ ] 所有任务清单中的任务已完成
- [ ] 任务清单状态已全部更新
- [ ] 代码可编译无语法错误
- [ ] 🛡️ 事后防护检查已执行（无🔴阻断项）

→ 完成后加载 `step-04_完成检查.md`
