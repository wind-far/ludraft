# 实测记录 · 2026-09-12

## 当前结论

本地工作台已从三角色基线扩展为八角色协作；隔离构建与浏览器验证、版本发布/修改/失败保护/回退/导出已实现。最新结果见文末八角色验证，前面的记录保留各阶段实测边界。**真实模型尚未配置，真实 LLM 生成链路和 15 次模型效果评测未运行。** 不将人工模板或固定协议响应记作 AI 生成成功。

| 检查 | 实际结果 | 证据边界 |
|---|---|---|
| 新增后端回归 | 26 通过 | 协议、审批、取消、修复上限、重启、密钥、路径、版本保护；使用模型/runner 替身的测试明确标注 |
| Docker 集成回归 | 1 通过，21.32 秒 | 固定模型协议响应 + 真实容器；创建、修改速度/生命/配色、三次真实编译失败、旧版保护、回退与导出 |
| 上游 verify_system.py | 62 通过 | 上游基础结构检查，不是完整 AI 游戏生产验证 |
| 前端 TypeScript | 通过 | 包含保留的上游页面 |
| 前端生产构建 | 通过 | 新入口 Studio.tsx |
| 三个示例 | 3 / 3 通过容器验证 | 人工编写的接物、躲避、点击模板 |
| 模板回归 | 15 / 15 构建和核心交互通过 | 5 种配置各 3 次；平均 2.84 秒，范围 2.41–3.65 秒；不是模型推理耗时 |
| 工作台浏览器检查 | 首页、模型表单、缺少模型的失败反馈、iframe 游戏启动已核验 | 独立来源 iframe 内可见计分和扣命 |
| 真实模型连接 | 未运行 | 没有配置模型 ID 与 API Key |
| 真实模型 5 × 3 评测 | 未运行 | 脚本已提供，配置模型后用 --live 执行 |
| 人工可玩性评分 | 未完成 | 结果文件保留 null，未捏造评分 |

完整逐次模板证据见 [template-regression.json](evidence/template-regression.json)。原始运行时文件位于 `.studio/evaluation-template/results.json`，包含重复的控制台日志。

## 模板回归覆盖

- collector，速度 320，3 条生命；
- collector，速度 180，3 条生命；
- collector，速度 320，5 条生命，蓝紫背景；
- dodger，速度 320，3 条生命；
- clicker，5 条生命，紫色背景。

每轮实际检查：TypeScript 编译、参数范围、开始、键盘移动、得分与 DOM 同步、危险目标扣命、游戏结束、重新开始、无浏览器异常。

## 运行环境及修正

macOS / Apple Silicon，Python 3.12.13，Node 24.18.0，Docker Desktop 29.7.2。Runner：Node 22 / Debian bookworm、TypeScript 5.9.3、Playwright 1.56.1 / Chromium Headless Shell 141。

Git 克隆超时后使用官方源码归档；Docker Hub 访问失败后使用 AWS 公共镜像源中的官方 Node 镜像；Debian HTTP 返回 502 后改为 HTTPS，保留证书与签名校验。首次 Documents 挂载被 macOS 权限拒绝后，runner 改为仅挂载候选项目的临时副本，未扩大 Docker 的个人目录权限。

测试过程中的失败记录保留并标记为「验证记录」，不伪装成成功游戏。

## 依赖与剩余限制

- 兼容范围内的 npm 审计修复已应用。锁文件仍有 2 项 moderate 提示，来自保留的旧 React Router 依赖；新版入口不使用 BrowserRouter，也不将其列入 vendor chunk。未为了旧页面做未经必要验证的大版本迁移。
- Python 测试有两条上游 Starlette/AnyIO 弃用提示，不影响本次通过结果。
- 受限模板不支持任意游戏类型、任意依赖安装或引擎 SDK；个性化玩法验收依赖人工试玩。


## 工作台功能扩展验证 · 2026-09-12

- 后端回归：37 通过；未启用 Docker 时 2 项集成测试跳过。新增覆盖项目别名、归档/回收站恢复、复制快照、玩法编辑与过期确认、参数范围、动态配置拒绝、调参失败及取消保护。
- 真实 Docker 集成：2 通过，20.70 秒。包含原有生成/修改/失败/回退/导出回归，以及新增无模型调用的参数调整、实际参数一致性验证和回退。
- TypeScript 类型检查及前端生产构建通过。
- 上述固定协议响应与测试替身均不计入真实模型效果评测；当前仍未完成真实模型端到端评测。

- 浏览器操作回归通过：方案编辑与确认、参数重置及保存、回退、重命名、复制、归档/恢复、回收站/恢复、搜索、参数失败后的旧版保护，以及 390px 手机布局。使用临时数据库和固定模型/runner 响应，不污染实际项目；无页面脚本异常。

## 使用引导与版本对比验证 · 2026-09-12

- 后端回归最新结果：**53 通过、2 项 Docker 集成跳过**。本轮未重跑 Docker 集成；上节的 2 项通过属于此前独立实测。
- 新增覆盖环境检查不调用模型、不泄露密钥或配置指纹；真实连接测试结果随配置失效；测试期间更换配置不得标为就绪；Docker 分级探测、超时清理、预览身份与来源限制；跨项目对比拒绝、同版对比、正反向差异、动态配置降级和活动版本保护。
- TypeScript 类型检查、生产构建通过。
- 临时数据库浏览器回归通过：首次引导、关闭后记忆、设置返回引导、显式连接测试、任意版本对比、交换方向、历史试玩返回、对比选择保留、相同版本提示与 390px 手机布局；无页面脚本异常。模型连接在该回归中使用固定响应，不能计为真实模型验证。
- 浏览器注入 HTTP 失败响应后，环境检查与版本对比均显示可见错误；重新检查或重新选择版本后恢复正常。该检查验证错误反馈，不是后端故障注入或模型测试。
- 更新后的实际本地诊断为 **5 / 6 项就绪**：API、Docker 命令、服务、镜像与预览身份通过，模型未配置。该检查没有调用模型。实际示例的单版本对比提示已验证；README 截图来自实际工作台。
- 真实模型生成链路、5 × 3 模型效果评测和人工可玩性评分仍未完成。


## 八角色协作验证 · 2026-09-12

- 最新后端回归：**71 通过，3 项 Docker 集成默认跳过**。包含八角色真实调用路径的固定协议测试、产物引用、上一版玩法与角色设计继承、确认后上下文更新、PM 依赖执行、并行失败清理、设计问题分派与依赖成果重建、主程/QA/工具/制作人门禁、取消、协议错误、模型覆盖配置、导出和旧流程兼容。
- 显式启用真实 Docker 集成：**3 通过，27.32 秒**。新增八角色创建与修改、两次可玩发布、三次故意 TypeScript 编译失败、旧版保护、回退及协作证据导出；同时复核既有生成和直接调参链路。角色返回为固定协议响应，构建和 Chromium 测试在真实容器执行。
- TypeScript 类型检查与前端生产构建通过。
- 浏览器回归通过：八个角色、编辑玩法后确认、12 项步骤产物、角色筛选、交接引用跳转、真实编译的 iframe 游戏启动、390px 布局和角色模型保存。使用临时数据库与固定协议响应，未修改实际作品和模型配置。
- 团队面板沿用现有工作台样式。截图 [ludraft-team.png](assets/ludraft-team.png) 展示测试数据，不能作为真实模型生成质量证据。
- 实际工作台已启用新流程；历史任务与直接调参保留其执行来源，不补造八角色产物。真实模型尚未配置，八角色真实模型端到端与 5 × 3 效果评测尚未运行。

当前边界：美术输出视觉规范而不生成图片；角色模型可单独指定 ID，但 API 地址与凭据共享；QA 额外验收仍需人工检查，固定工具测试通过不代表满足全部玩法条件。


## 协作问题修复验证 · 2026-09-12

- 后端完整回归：**79 通过，3 项 Docker 集成默认跳过**。新增 8 项测试覆盖本轮完整设计交接及修订版本引用、复制/调参来源链、旧记录兼容、ZIP 证据引用、循环终止、失败响应与超时元数据、无调用预检以及自定义适配器协议错误用量。
- 显式 Docker 集成：**3 通过，25.28 秒**，覆盖生成、修改、调参、失败保护、回退及导出。模型仍使用固定测试协议响应。
- TypeScript 类型检查及生产构建通过。
- 隔离浏览器回归通过：八角色流程、玩法编辑确认、12 项产物、交接导航、真实容器游戏启动、角色模型保存；复制项目显示 12 项来源交付物，八角色仍标记未执行；390px 布局无横向溢出，无页面脚本异常。临时数据库未污染实际作品。
- 真实模型生成质量、5 × 3 效果评测和人工可玩性评价仍未完成；上述结果不替代真实模型评测。


## 多类型任务路由验证 · 2026-09-12

- 后端完整回归：**92 通过、4 项 Docker 集成默认跳过**。新增 13 项覆盖四类执行范围、自动分类、用户指定类型、审批拒绝、新作品/缺失设计/编辑方案时的范围扩展、PM 扩展及依赖传播、沿用设计修复后的引用闭包、工具失败保护和接口参数校验。
- 显式真实 Docker 集成：**4 通过，36.74 秒**。新增视觉调整路由，沿用技术设计、执行 11 次角色调用，真实构建/交互验证及来源完整的 ZIP 导出通过；同时回归原有生成、修改、调参和失败保护。
- TypeScript 类型检查、生产构建通过。临时数据库浏览器验证：任务类型选择、修复问题建议与执行安排、审批后 10 次当前调用、美术/UX 显示沿用、来源跳转、真实容器游戏启动及 390px 布局，无页面脚本异常。
- 上述调用数量来自固定协议测试响应，不是模型质量或成本评测。真实模型端到端、专项性能基准及 5 × 3 效果评测仍未执行。

## 动态编码与三消目标验证 · 2026-09-12

- 用户指定目标游戏为三消。新增独立 8×8 三消模板，默认需求、灵感卡片、参数与评测需求均围绕三消；保留旧作品兼容。
- 后端完整回归：**104 通过、5 项 Docker 集成默认跳过**。动态任务覆盖 DAG 校验、同文件写入顺序、独立并行、归属越界、整批失败不落盘、取消、系统合并及引用闭包。三消参数与模板选择、调参发布、算法随机回归通过。
- 最终真实 Docker 集成：**5 通过，33.56 秒**。覆盖旧流程、按类型路由及三消的主程分工、两个程序子任务、系统合并、真实点击消除/无效回退/连锁/死局重排/胜负/重开。
- 算法测试使用独立匹配检查器，验证四/五/六色的 **90 组固定种子游戏**，以及退化随机源、非法索引、步数结算与重开；另独立验证 runner 的无效交换及恰好两轮连锁棋盘。
- 五条三消模板需求各三次：最终 **15/15 构建通过、15/15 核心交互通过**，逐次总耗时 **74.29 秒**。[最终原始结果](evidence/match3-template-final/results.json)。这是模板回归，不是模型生成评测。
- 保留[首次批量结果](evidence/match3-template/results.json)：五色场景 3 次未通过，因为测试误把可形成纵向三连的位置作为无效交换。已修正测试位置，并对所有宝石种类独立核验；连锁棋盘还单独保留了另一组合法交换，避免死局重排干扰下落位置断言。
- 前端类型检查及生产构建通过。临时数据库浏览器验证三消生成、子任务展示、系统合并、拖动交换得分、步数/目标/宝石种类调参和 390px 布局；正式示例的移动触屏点击消除与每次只扣一步也通过。
- 正式工作台已导入宝石花园、糖果连锁、星夜挑战，均先经真实容器验证，标注人工模板、不补造 Agent 历史。截图已更新为当前正式示例。
- 真实模型尚未配置，模型生成的代表案例、5×3 模型效果评测和人工可玩性评价仍待完成。完整复刻剩余工作见 [验收清单](REPLICATION_CHECKLIST.md)。

## 消息与澄清验证 · 2026-09-13

- 后端完整回归：**117 通过、5 项 Docker 集成默认跳过，2.94 秒**。新增 13 项测试覆盖定向/广播上下文与引用导出、同一步骤去重、跨运行隔离、模型并发槽排队期间投递、策划/设计/编码/交付阶段提问、并行取消、全量答复后恢复、重启持久化、达到消息上限仍可答复、模型/测试执行中补充需求，以及相同玩法文本下旧审批和旧编辑的失效。
- 显式真实 Docker 集成：**5 通过，34.38 秒**。回归三消、历史模式、任务路由、版本保护与导出。
- 前端 TypeScript 类型检查和生产构建通过。浏览器在隔离数据库中操作三消生成→UX 提问→答复→重新确认→真实容器验证→试玩；验证定向消息、广播、上下文投递记录、问题已答复状态及 390px 无横向溢出，无页面脚本异常。
- [澄清页面](assets/ludraft-clarification.png)、[消息记录](assets/ludraft-messages.png) 使用固定模型协议响应、真实容器和真实浏览器，仅证明流程运行。正式数据未导入这些测试任务。
- 审查与工具结果不能证明全部消息已落实；界面仅标记具体上下文投递。已完成角色不会因留言自行再次运行。真实模型调用、15 次模型效果评测及人工可玩性评价仍待完成。

## 独立模型连接验证 · 2026-09-13

- 后端完整回归：**131 通过、5 项 Docker 集成默认跳过，4.06 秒**。新增 14 项连接测试覆盖默认继承/独立凭据、OpenAI/DeepSeek 路由、Anthropic 请求与缓存用量、错误与格式失败、旧配置迁移、0600 原子文件、地址变更拒绝沿用密钥、清除与切回默认、本地无密钥服务、测试指纹失效、测试期间改配置、输入错误不回显密钥、交付来源记录及取消中的请求元数据。
- 显式真实 Docker 集成：**5 通过，34.89 秒**，回归三消、旧游戏、路由、验证及版本门禁。
- TypeScript 类型检查及生产构建通过。浏览器用生产 Gateway 连接本地 HTTP 协议模拟端点，配置独立 Anthropic 程序角色，测试全部八角色，验证密码输入清空、GET 不含凭据、改变地址拒绝沿用旧密钥、切回默认清除独立密钥及测试失效。桌面双列设置及 390px 布局通过，无页面脚本异常。[设置截图](assets/ludraft-role-connections.png)。
- 浏览器第一次运行因设置入口有两个同名按钮导致测试脚本严格选择器失败；改为选择侧栏入口后完整通过。未把该次脚本失败当作产品成功。
- 所有连接响应来自固定协议模拟，不是外部真实模型验证。正式工作台仍需配置可用服务后完成八角色实际任务、代表案例和 15 次效果评测。

## 规则与技能加载验证 · 2026-09-13

- 完整回归：**141 通过、5 项 Docker 集成默认跳过，10.73 秒**。新增 10 项覆盖八角色/各步骤来源映射、真实提示词包含规则、自定义技能与角色版本、调用快照不可变、历史恢复及角色隔离、过期编辑拒绝、非法路径与符号链接、规则缺失时不发起模型请求、正文 SHA-256、导出引用与上游许可，以及自定义指令不能解除工具失败门禁。
- 显式真实 Docker 集成：**5 通过，36.92 秒**，三消及旧游戏流程、路由、构建测试与版本保护均通过。
- TypeScript 类型检查与生产构建通过。隔离数据库浏览器验证：创建自定义三消技能→角色选用和补充规则→确认三消玩法→真实容器生成验证→查看实际调用快照→编辑设置后原快照不变→恢复历史设置；390px 下规则入口可用且无横向溢出，无页面脚本异常。
- [角色规则设置](assets/ludraft-role-rules.png)、[调用快照](assets/ludraft-rule-snapshot.png)来自协议测试响应和真实容器/浏览器，不能作为外部模型已遵循这些规则的效果证明。
- 上游 Unity/C# 文档作为有来源的参考，默认选用本地三消技能；没有因此接入 Unity、任意文件读写或额外命令执行。后续仍需配置真实模型完成代表案例与 15 次效果评测。

## 三消需求材料验证 · 2026-09-13

- 完整后端回归：**155 通过、5 项 Docker 集成默认跳过，12.14 秒**。新增 14 项覆盖中文 DOCX 段落/表格/页眉、PDF 文字、ZIP 部分失败、路径与符号链接、数量/展开/截断限制、XML 实体拒绝、乱码及空文档、子进程环境和超时/取消清理、人工核对稿进入各角色上下文、不可变引用、导出、删除限制与数据库写入失败后的原件清理。
- 显式真实 Docker 集成：**5 通过，35.27 秒**，回归三消与历史游戏、任务路由、构建/交互、失败保护及导出。模型使用固定协议响应。
- TypeScript 类型检查、生产构建及已跟踪文件的 diff 空白检查通过。
- 隔离数据库浏览器通过：上传含中文 Word、文字 PDF 和损坏 PDF 的 ZIP→核对提取稿→上传文件夹并保留相对名称→选择两份材料→创建三消需求→确认→真实 Docker 构建/测试→浏览器试玩。上传阶段没有创建项目或启动模型任务，损坏成员错误可见，运行材料与核对稿一致；390px 页面无横向溢出，无脚本异常。
- [材料界面截图](assets/ludraft-materials.png)使用测试材料及固定模型协议响应；材料解析、容器和浏览器为实际执行。测试任务保存在临时数据库，没有导入正式作品。
- 开发中发现并修正两处失败：上传记录 INSERT 占位符数量错误导致接口失败；macOS 当前 Python 环境无法设置所用地址空间限制，改为峰值 RSS 监测并明确其不是内核硬限制。另补充保存故障时清理未登记原件，防止材料库不可见的文件残留。
- 尚不支持 OCR、DOC/RAR、嵌套 ZIP、版式还原；提取结果必须核对。真实模型代表案例、5×3 模型效果评测及人工可玩性评价仍未完成。

## 三消配置调整路径验证 · 2026-09-13

- 完整后端回归：**168 通过、6 项 Docker 集成默认跳过，17.81 秒**。新增 13 项覆盖 PM 参数提案、确认前不改文件、严格配置范围、参数/名称/动态表达式拒绝、实际工具与 QA/制作人阻断、两轮修复上限、取消并等待调用、旧审批失效、消息重策划、等待确认重启、提案编辑保护、空项目拒绝、无变化拒绝、旧游戏兼容及来源/规则导出。
- 显式真实 Docker 集成：**6 通过，51.73 秒**。新增三消 CONFIG 路径，确认 30 步 / 1500 分后真实编译和浏览器验证通过，game.ts 保持原样，实际参数一致；来源引用闭合、ZIP 及回退可用。另回归已有五条集成流程。
- TypeScript 类型检查、前端生产构建及已跟踪文件 diff 空白检查通过。
- 浏览器在临时数据库完成：创建三消可玩版本→选择配置调整→核对 20→30 步、1200→1500 分→补充为 35 步→重新核对确认→真实 Docker 验证→新版 iframe 开始游戏。确认前活动版本未改变；保留历史设计，390px 布局无横向溢出，无脚本异常。[参数提案截图](assets/ludraft-config-proposal.png)。
- 首次新增接口测试将消息创建状态码误写为 200，按真实接口 201 修正后通过。首次浏览器断言使用 option 的 isDisabled 返回 false；核验实际 HTML 含 disabled 后，改用 DOM disabled 属性检查，完整流程通过。这两次失败未算作通过。
- 所有模型响应来自固定协议夹具，实际容器与浏览器运行不等于真实模型生成质量。配置路径无修复时执行 6 次调用，来自制作人、PM、程序和 QA；八角色团队保留，按任务职责执行，不补造其他角色记录。
- 仍待完成：TEST / DOC / REVIEW / RESEARCH 独立流程、真实模型代表案例与 5×3 评测、人工可玩性评价及最终整体验收。

## 独立测试与报告验证 · 2026-09-13

- 完整后端回归：**184 通过、7 项 Docker 集成默认跳过，23.33 秒**。新增 16 项覆盖测试范围、确认前不测试、没有新版本、完整输入摘要、缺失/无效检查拒绝、失败及 QA/交付/模型错误报告、输入变化、取消并等待、旧审批、重启审批、范围编辑拒绝、QA 澄清后重新确认、不可变报告/规则摘要、下载来源以及后续修复引用。
- 显式真实 Docker 集成：**7 通过，60.45 秒**。新增独立三消测试通过及故意损坏候选副本导致真实 TypeScript 编译失败；两种报告均可导出，失败不修复、不发布，基准文件和活动版本未变。其余六条集成同步回归。
- TypeScript 类型检查、前端生产构建及已跟踪文件 diff 空白检查通过。
- 临时数据库浏览器通过：创建三消版本→选择独立测试→确认版本与范围→真实容器测试→查看报告并下载 ZIP。再次发起 QA 阻断测试，界面明确区分工具通过与报告未通过；失败报告可下载，原版本仍可启动。390px 无横向溢出，无脚本异常。[报告截图](assets/ludraft-test-report.png)。
- 实际下载 ZIP 含 8 个文件，报告有 13 项工具检查；内容 SHA-256 及全部本轮规则引用核验通过。图片和测试返回均使用固定模型协议；实际容器/浏览器证据不代替真实模型效果评测。
- 待补：DOC / REVIEW / RESEARCH 独立流程、真实模型代表案例与 5×3 评测、人工可玩性及最终整体验收。

## 独立文档流程验证 · 2026-09-13

- 完整后端回归：**198 通过、7 项 Docker 集成默认跳过，23.91 秒**。随后补充标题/文件 ID 校验，文档与角色规则定向回归 **25 通过，3.23 秒**（文档用例共 15 项）。覆盖新项目无 Docker/无游戏版本、引文/来源/行号/空正文/章节/编号错误、PM 拒绝与模型失败、两轮上限、成功文档不变性、修订成功、文档历史与后续生成引用、ZIP、拒绝后结束、范围重规划、重启、旧审批、取消和澄清后重新确认。
- 显式真实 Docker 共享链路回归：**7 通过，59.39 秒**。三消生成、修复、参数与独立测试仍经过真实构建与浏览器工具检查；文档流程自身不执行游戏构建或测试。
- TypeScript 类型检查与生产构建通过。构建文件：`index-0CdWcunS.css`、`index-DpPw1nk1.js`。
- 临时数据库浏览器通过：新项目选择文档→确认文档与章节→查看正文→展开引文原文→下载 ZIP→继续修改并引用前份文档。历史结果未改变、游戏版本数保持 0；390px 无横向溢出，无脚本异常。[文档截图](assets/ludraft-documents.png)。
- 实际下载 ZIP 核对 10 个条目、4 次角色调用、来源及报告 SHA-256；模型均为 `document-protocol-fixture` 等固定协议响应，不作为真实模型效果证据。
- 七类任务流程已实现；REVIEW / RESEARCH 独立流程、真实模型代表案例与 5×3 评测、人工可玩性及最终整体验收仍待完成。

## 独立代码审查验证 · 2026-09-13

- 完整后端回归：**220 通过、7 项 Docker 集成默认跳过，31.51 秒**。随后补充跨类型引用闭包，审查/文档/测试报告定向回归 **53 通过，12.67 秒**（代码审查用例共 22 项）。覆盖问题成立/排除/待人工判断/无问题、引用行号/原文/范围、文件覆盖和重复编号、回应缺失及无效引文、复核拒绝、模型失败、两轮修订、输入变化、历史不变性、指定旧报告修复、版本不匹配、重规划/重启/旧审批、取消、澄清与跨类型导出引用。
- 真实 Docker 共享流程回归：**7 通过，63.08 秒**；没有用静态审查代替实际游戏构建与交互测试。
- TypeScript 类型检查与前端生产构建通过，构建文件为 `index-0CdWcunS.css`、`index-Cbsxcx6o.js`。
- 临时数据库浏览器：实际容器生成三消基线→确认静态审查版本/文件→查看引用、程序回应和主程复核→下载报告→从所选报告填写修复需求→确认后实际容器验证新版本→试玩。审查阶段版本数不变，修复单独创建新版本；原报告保持不变，390px 无横向溢出，无脚本异常。[审查截图](assets/ludraft-code-review.png)。
- 下载审查 ZIP 的 9 个条目、5 次角色调用、来源和报告 SHA-256 已核对；新游戏 ZIP 保留明确选定的审查报告引用与真实三消工具证据。
- 模型与问题采用固定协议场景；截图中的「三消边界复核演示」不是对真实模板缺陷的判断，也不证明模型已准确发现或修复实际缺陷。真实模型质量评测仍待完成。
- 八类任务路径已实现；RESEARCH、真实模型代表案例与 5×3 评测、人工可玩性及最终整体验收仍待完成。

## 方向调研与公开网页验证 · 2026-09-13

- 完整后端回归：**258 通过、7 项 Docker 集成默认跳过，26.84 秒**。随后补充澄清重审批和慢响应超时，调研/网页/文档定向回归 **54 通过，3.68 秒**。覆盖审批前不读取网页、固定范围比较、事实缺少引用、问题/方向/维度错误、推荐越界、模型/复核/网页失败、两轮上限、取消、来源刷新、选择非推荐方向、旧基准拒绝、报告与所选方向导出。
- 修复已发现的材料字段错误：文档/审查/调研来源目录使用 `reviewed_text`，回归验证人工核对的 30 步稿进入文档来源，而非原始提取的 20 步稿。
- 网页边界验证：阻断私网与本地 URL、凭据、非标准端口、混合私网 DNS、恶意重定向、超量/不支持/压缩/空内容；IP 固定、Host 与 TLS SNI、去除脚本样式、取消/超时关闭响应、跨站不转 Cookie、显式截断均有测试。
- 真实 HTTPS 读取 `https://example.org/` 成功，提取 142 个字符并保存摘要。它仅用于验证网络读取，不作为三消市场事实来源。
- 浏览器：新项目确认调研范围→真实公开网页读取→显示引用及统一维度比较→导出报告→选择非推荐的「连锁挑战」→再次确认玩法→真实容器构建三消→试玩。调研阶段没有游戏版本，采用方向后单独生成 1 个版本；原报告不变。390px 无横向溢出，无脚本异常。[调研截图](assets/ludraft-research.png)。
- 下载 ZIP 核对 9 个条目、来源和报告 SHA-256；游戏 ZIP 保留准确的报告/方向选择及真实三消工具证据。TypeScript 与生产构建通过：`index-RZkRLpxx.css`、`index-BOtqkqcD.js`。
- 真实 Docker 共享回归：**7 通过，52.91 秒**。调研本身不执行游戏测试。
- 所有模型结论采用固定协议响应，不证明真实研究质量。九类任务路径已接入，仍需真实模型代表案例、5×3 效果评测、人工可玩性和最终整体验收。网页功能是用户指定来源读取，不是全网搜索。

### 多类型定位恢复（2026-09-13）

- 用户澄清三消只是其中一种；首页提供接物、躲避、点击、三消入口，策划确认前不预选模板。新建调研/文档的审批占位模式不再决定游戏合同或模板代码。
- 四种模式的新建八角色流程均通过固定协议回归；这是调度与门禁证据，不是实际模型效果评测。
- `scripts/evaluate.py --output .studio/evaluation-mixed-20260913`：五条固定需求 × 三次，真实 Docker 构建 **15/15**、对应模式核心交互 **15/15**、明确参数 **15/15**。累计运行 65.42 秒，修复 0 次，模型调用 0，人工评价 0。
- 本次用例：宝石花园（match3）、星光收集站（collector）、流星闪避（dodger）、点亮星星（clicker）、糖果连锁（match3）。历史三消专项文件保持原样。
- [跨类型逐次证据](evidence/mixed-template-regression.json)记录全部检查；完整游戏 ZIP 和摘要在本地 `.studio/evaluation-mixed-20260913/artifacts/`。
- 评测新增模式/参数校验、八角色和版本检查、澄清阻断、每轮证据与 token 缺失口径、独立人工评分导入。配色语义、完整需求符合度和趣味性仍需人工评价。
- 当前八角色有效模型配置均为空，真实模型生成和五条需求 × 三次的模型评测尚未完成；没有以模板结果替代。

本轮最终回归：`pytest tests -q` **276 passed, 7 skipped**（40.43 秒；7 项为 opt-in Docker 集成，本轮另跑了上述 15 次真实 Docker 模板回归）。前端 TypeScript 检查与生产构建通过；浏览器验证首页四个玩法入口和躲避示例填入需求，保留原工作台视觉风格。


### 发布门禁一致性修复（2026-09-13）

审计发现生成/改参依赖工具成功标记，完整证据校验只在独立测试与评测执行。现已统一全部发布入口，并在最终发布处复核，防止不完整成功标记生成版本。

- 新增四种玩法 × 七类故障回归：检查缺失、检查结果冲突、构建失败、异常退出、缺退出码、无效响应、工具异常。确认初次执行加两轮修复后失败，三轮 QA 都收到失败证据，没有制作人交付或可玩版本。
- 覆盖自然语言配置、直接参数面板、历史生成路径和直接调用发布；失败保留已有可玩版本。
- 全套 Python 回归 **312 passed, 7 skipped**（42.41 秒）；另行启用真实 Docker 集成 **7 passed**（63.84 秒），覆盖生成、修改失败保护、回退、源码导出、八角色和三消流程。
- 补充实际 React 组件渲染回归，验证完整、缺项、无效字段、冲突重复项、错误退出及字符串伪布尔结果。与门禁故障回归合计 **37 passed**（7.93 秒）。前端类型检查与生产构建通过。
- 当前有效八角色模型配置仍为空。本次是协议、发布门禁和真实容器验证，不是外部模型质量验收；完整目标继续进行。


### 手机试玩与独立导出（2026-09-13）

- 新 Canvas 固定入口支持主指针拖动，松手、取消和失焦停止；兼容大小写 A/D。接物、躲避、点击显示剩余秒数。三消补齐主指针与取消管理。
- runner 新增 390×844 Chromium 触屏上下文，验证布局、拖动停止/取消、点击计分、三消交换、倒计时结束与重开。旧 Canvas 快照保留桌面测试，手机能力明确为未验证。
- 首轮触屏用例中，部分断言早于 requestAnimationFrame 更新界面。修正为等待计分/倒计时 DOM 实际刷新后检查，没有移除验收项；失败记录保存在 `.studio/evaluation-touch-20260913`。
- 最终五条需求 **5/5** 构建、桌面/手机核心交互、明确参数通过，累计 24.50 秒。证据见 [移动端模板回归](evidence/mobile-template-regression.json)，完整源码 ZIP 在 `.studio/evaluation-touch-final-20260913`。模型调用为 0，人工评分为 0。
- 真实 Docker 集成 **11 passed**（108.84 秒）：原七项，加四种模式导出/解压后仅使用导出文件在独立容器和 HTTP 服务中构建、试玩与触屏验证。没有使用工作台 API 或模型密钥。
- 每个游戏导出包附带 `RUN_GAME.md` 和 `licenses/upstream-MIT.txt`，已有 dist 时只需静态服务器即可运行。更新原精确 ZIP 文件列表断言后，发布/导出/测试报告/门禁相关回归 **81 passed**（11.66 秒）；全套此前有 316 项通过和该一项预期列表失败，11 项 opt-in 跳过。前端类型检查与构建通过。
- 浏览器另核查导出接物游戏的 390px 画面：计分、生命、倒计时、按钮无横向溢出，开始后显示进行中。此观察不代替用户人工可玩性评分。

### 交付审计与跨类型回归（2026-09-13）

- 修复三消参数与其他玩法字段不一致导致版本对比报错；四种模式的 16 种组合、反向比较和同版本比较通过，缺少的模式专属参数显示“不适用”，比较不改变活动版本。
- 环境诊断核对八角色生效连接；仅完全一致的连接身份可共享有效连接测试记录。独立角色未测试、失败或凭据变化后不再误报就绪。共享连接验证不等于八角色实际执行。
- 实际 ASGI SSE 响应测试覆盖首次回放、订阅后新增事件、跨任务隔离、Last-Event-ID 与 after 游标、数据库重开后续读、断开清理、无效游标和不存在的任务；未以此声称完成浏览器 EventSource 全链路测试。
- 相关定向回归 **50 passed**（2.22 秒）。最终全量回归 **337 passed, 11 skipped**（40.41 秒）；11 项为 opt-in Docker 测试，前一轮已显式执行并全部通过（108.84 秒），本轮未修改容器代码。
- 前端 TypeScript 检查与生产构建通过，输出 `index-CqaQYTyt.css`、`index-CrmZpD0I.js`。
- 逐项缺口见 [完整交付审计](DELIVERY_AUDIT.md)。真实模型生成、九类模型联调、五条需求各三次模型评测及人工可玩性评价仍未完成。
- 正式服务已重启加载本轮修复；实时诊断确认 API、Docker 命令/服务/镜像、独立预览均通过，八角色均缺少模型或所需凭据，整体 ready=false；诊断没有调用模型。

### 用户参考图的画布界面调整

- 实现创作首页、空白画布和项目画布三个状态；保留原有审批、错误、团队、消息、参数、源码、版本、验证及报告组件。未调用生成模型，未重写已有游戏快照。
- 浏览器验证：新建画布→打开需求编辑；真实三消/点击示例接入；三消开始后打开参数面板，游戏仍显示重新开始，未因面板切换重新加载；版本入口可查看当前版本与回退禁用状态；八角色日志如实标示模板示例未运行模型。
- 参考图片经实际文件选择器保存，重新载入项目后恢复；未串到另一项目。拖动参考卡片前坐标约 (206,217)，拖动后约 (246,252)；平移前 transform 为 matrix(0.85,0,0,0.85,0,0)，平移后为 matrix(0.85,0,0,0.85,50,35)。缩放与适应画布控件可用。
- 390px 首页及项目页面 scrollWidth 均为 390；参数面板横坐标 8–382，没有横向溢出。桌面 1440×1000 核查并保存首页、项目截图；保留独立手机截图。修复了旧样式对手机标题的 115px 限宽及工具栏遮挡。
- 定向回归 `pytest tests/test_discovery.py tests/test_verification_ui.py -q`：**35 passed**（1.16 秒，2 项已有依赖弃用警告）。最终 TypeScript 与生产构建通过：`index-DaA32Rvw.css`、`index-CJZ-jog3.js`。本轮没有重跑 Docker 和真实模型评测。
- 页面控制台检查未见脚本错误。新图为首页装饰，参考图与实际游戏渲染有意分别展示；图片生成不是工作台已接入的用户能力。

### 2026-09-13 首页标题反馈修正

- 将首页主标题移入顶部，移除重复的“游戏创作工作台”及两段引导语，正文直接从花园横幅开始。手机端保留导航内的模型配置入口。
- 浏览器实测 952×644 和 390×844：首页只有一个 h1，均无横向溢出；桌面横幅顶部为 76px，手机标题完整显示，导航内可见“模型与连接”。更新首页桌面及手机截图。
- TypeScript 检查、生产构建通过，输出 `index-BP6uCco3.css`、`index-DUO7tl5g.js`。本次仅调整首页展示，未重跑后端或模型评测。

### 2026-09-13 导航底部本地身份

- 替换侧栏底部存储提示为默认人物图标及“本地创作者 / 本地工作空间”，移除画布的 W 占位头像。
- 浏览器实际 CSS 视口 865×585、354×767（当前浏览器缩放下）检查：身份区完整位于侧栏底部，均无页面横向溢出，手机导航打开后可见。保留手机侧栏截图。
- TypeScript 与生产构建通过；本次没有新增登录、个人资料存储或账户操作。

### 2026-09-13 首页内容层级复核

- 取消首页全局顶栏，将唯一 h1 放入创作内容容器，移除首页右上角重复模型配置按钮；保留其他页面工具栏。删除上一轮 home-topbar 样式，统一标题与横幅的内容边界。
- 浏览器实测：首页 main 下 topbar 数量为 0，h1 数量为 1；标题与横幅左边界一致，无横向溢出。侧栏“模型与连接”可正常打开设置，未修改或提交配置；手机导航中该入口可见，标题完整。更新桌面与手机首页截图。
- TypeScript 与生产构建通过，未运行模型或后端评测。

### 2026-09-13 角色规则弹窗布局

- 改为固定头部与独立滚动正文；标题为“角色规则与技能”，关闭按钮位于头部右侧，隐藏正文滚动条并保留滚动能力。
- 浏览器实测正文 clientHeight=548、scrollHeight=847；滚动到 scrollTop=299 后，关闭按钮 top 保持 49px，保存按钮 bottom=654.97px，小于正文 bottom=695px。确认底部操作可达且关闭按钮不随内容滚动。
- TypeScript、生产构建通过。后续截图、点击关闭及手机回归受浏览器自动审批连续超时影响，未完成，不计为通过；未修改或保存任何角色规则。
