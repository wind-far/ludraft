# 完整复刻与迭代验收清单

目标：复刻上游八 Agent 协作能力，并交付可靠的本地多类型网页游戏工作台。保留现有界面风格、上游许可与来源。对照上游 main（初始导入 dd631f6）源码及 README；不把骨架和模拟结果当作已实现业务能力。

本清单跟踪完整目标，不能用其中一个阶段通过代替项目完成。初始约束仍有效：本地单用户、FastAPI/React/SQLite、TypeScript Canvas；Unity/Unreal、账户计费、多人在线和 AI 美术生成不在当前交付范围。

| 验收项 | 当前状态 | 完成所需证据 |
| --- | --- | --- |
| 八角色真实模型协议、产物与交接引用 | 已实现，真实模型联调待完成 | 每个角色真实响应、用量、交接记录 |
| 多需求类型路由 | 九类已接入；协议、工具与 UI 验证通过，真实模型待完成 | 对照九类上游需求，逐类运行及 UI 验证 |
| 主程动态拆解编码任务、程序并行与合并 | 已实现，协议与真实容器验证通过 | DAG、文件归属、依赖执行、合并冲突、取消和真实构建测试 |
| Agent 消息队列、定向/广播及消费记录 | 已实现，投递与 UI 验证通过 | 跨步骤读取、并行投递、去重、重启和 UI 验证 |
| 用户中途消息与 Agent 请求澄清 | 已实现，暂停/回复/重新确认验证通过 | 真实暂停/回复/继续，旧审批失效，取消与重启 |
| 原版角色规则与技能加载、查看和自定义 | 已实现，实际上下文/历史/导出/UI 验证通过 | 实际模型上下文与规则版本对应、编辑权限、来源记录 |
| 每角色独立模型/服务商/凭据 | 已实现，协议与本地 HTTP/UI 验证通过；外部模型待配置 | 脱敏接口、调用适配、每角色连接验证 |
| 文档/文件夹上传与需求提取 | 已实现，解析/人工核对/引用导出/UI 验证通过 | md/pdf/docx/zip、限制与失败反馈、任务引用；扫描 PDF 不支持 OCR |
| 需求确认、生成、修复、试玩 | 已统一完整工具证据及最终发布门禁，模型实测待完成 | 真实模型到容器到浏览器交互 |
| SSE、状态、取消、错误与持久化 | 状态回归与实际 ASGI SSE 游标续传、任务隔离、数据库重开验证通过 | 最终真实模型工作流走查 |
| 源码查看、版本对比/回退/导出 | 四种玩法 16 种对比组合、版本不变性与四种 ZIP 独立运行验证通过 | 真实模型连续迭代案例 |
| 搜索、重命名、复制、归档/恢复 | 已实现 | API 与 UI 回归 |
| 容器资源限制、密钥隔离、独立来源预览 | 已实现，随新功能复验 | 真实容器参数、来源限制、失败门禁 |
| 三个代表案例、架构与贡献说明 | 四类模板案例已有，模型案例待补 | 可复现模型生成记录与人工试玩 |
| 五条需求每条三次评测 | 待真实模型 | 构建/交互/修复次数/耗时/token 原始记录；人工独立评价 |
| 最终完整回归、桌面/手机验收、README 更新 | 337 项全量回归通过；四种模板触屏、导出独立运行已验证；见 DELIVERY_AUDIT.md | 真实模型与人工试玩后重新审核 |

真实模型配置缺失时，继续实现和验证其他能力；不得用固定响应替代最后两项真实模型交付。模板测试、协议替身和实际模型结果必须分别标注。

用户最新澄清：三消只是其中一种。当前四种玩法（接物、躲避、点击、三消）均支持新建与迭代，需求决定模式；八 Agent 协作与现有工作台风格保持。默认评测覆盖四种模式；历史三消专项证据保留，不用于勾选真实模型评测。

消息语义：定向/广播进入后续匹配角色调用，上下文记录不代表消息落实；不自动唤醒已完成角色。澄清回复后从策划流程重启，必须再次审批。
