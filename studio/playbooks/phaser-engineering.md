# Phaser 工程交付与审查

- 读取本轮模板清单、已确认玩法、主程分工和必要代码；按场景、实体、配置及资源键核对真实接口。
- 当前首先接入固定路线塔防。诊断样例通过不代表塔防整局或连续修改已验收。
- 使用工具分页读取相关文件、按源摘要提交增改删；不假设工程只有三个文件，不将全部源码重复放入每个角色上下文。
- 保留既有规则、资源键、未分配文件与受保护核心。不要为绕过工具限制修改依赖、测试、加载器或系统配置。
- 编码分工可以包含多个任务，同一工程由执行器串行实现。后续任务必须读取前序实际变更，不能覆盖之前的工作。
- 减速、冻结、升级等新机制需要明确持续时间、叠加规则、目标生命周期与重开复位，并接入实际游戏场景。
- 构建诊断来自真实工具；审查需核对源码与确认规则，完整玩法验证另由可信验证器执行。
- 本轮无法完成时说明缺少的接口或证据，不能将 CLI 成功退出当作游戏已交付。
